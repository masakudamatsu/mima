import React from 'react';
// import PropTypes from 'prop-types';

export const SearchBox = ({children}) => {
  return (
    <>
      <div>Search Box</div>
    </>
  );
};

// SearchBox.propTypes = {};
