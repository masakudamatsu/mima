import styled from 'styled-components';
import {dimension} from 'src/utils/designtokens';

export const ParagraphMenu = styled.p`
  padding: ${dimension.button['minimum target spacing 50']} 0; /* Same as menu list items; see ListMenu.js */
`;
